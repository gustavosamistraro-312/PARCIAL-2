# Recebe as horas
h = int(input("Horas: "))

# Recebe os minutos
m = int(input("Minutos: "))

# Recebe os segundos
s = int(input("Segundos: "))

# Converte tudo para segundos
total = h * 3600 + m * 60 + s

# Mostra o total final
print("Total em segundos:", total)
